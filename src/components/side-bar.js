import styled from "styled-components";

const Wrapper = styled.section`
    display: flex;
    width: 300px;
    height: 100%;
`;

const SideBar = () => {
    return (
        <Wrapper/>
    )
}

export default SideBar;